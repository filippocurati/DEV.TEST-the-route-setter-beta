import { describe, expect, it } from 'vitest';

describe('suite fisica headless', () => {
  it('esegue il runner deterministico predisposto per le fasi fisiche', () => {
    expect(1).toBe(1);
  });
});
